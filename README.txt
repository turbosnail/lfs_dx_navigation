подключение библиотеки.
инициализация навигационной системы навигационная система имеет следующие методы
// Все что относится к работе с длл
SetMap("WE")
RotateMap(bool flag)
SetPlayerPosition(x,y)
SetPlayerRotation(degres)
ClearRoute()
bool SetRoute(Xfrom, Y from, Xto, Yto)



// Все что относится к спрайту
SetPosition(float x, float y);
SetSize(float width, float height)
SetMap(char* mapName);
SetMapCentr(float x, float y);
SetMapRotate(float degres);
SetMapScale(float scale);